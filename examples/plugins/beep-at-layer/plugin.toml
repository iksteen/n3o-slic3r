name = "beep-at-layer"
version = "0.1.0"
entry = "main.lua"
hooks = ["post_slice"]
description = "Beep (M300) at the start of a chosen layer."

[settings.layer]
type = "number"
default = 1
label = "Layer"
