name = "pause-at-layer"
version = "0.1.0"
entry = "main.lua"
hooks = ["post_slice"]
description = "Pause the print at the start of a configured layer."

[settings.layer]
type = "number"
default = 1
label = "Layer"
