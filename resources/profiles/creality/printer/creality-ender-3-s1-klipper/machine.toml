# DERIVED variant of `creality-ender-3-s1` — regenerated by
# scripts/derive_printer_variant.py; edit the base profile or the
# derivation call in scripts/sync_orcaslicer.sh, not this file.
# Base machine — keys shared across every nozzle variant.
# Generated by scripts/import_machine_profile.py from:
#   external/OrcaSlicer/resources/profiles/Creality/machine/
# Per-nozzle scalars live under `creality-ender-3-s1/nozzles/<sku>.toml`.

auxiliary_fan = "0"
bed_exclude_area = "0x0"
# Not authored upstream (Creality profiles predate Orca's bed-type
# picker). Textured PEI is the project default for new printers —
# the near-universal sheet in practice; the stock PC plate stays
# available in beds/.
default_bed_type = "Textured PEI Plate"
before_layer_change_gcode = ''';BEFORE_LAYER_CHANGE
;[layer_z]
G92 E0
'''
change_filament_gcode = "M600"
extruder_clearance_height_to_lid = "34"
extruder_clearance_height_to_rod = "34"
extruder_clearance_radius = "47"
gcode_flavor = "klipper"
layer_change_gcode = ""
machine_end_gcode = '''{if max_layer_z < printable_height}G1 Z{z_offset+min(max_layer_z+2, printable_height)} F600 ; Move print head up{endif}
G1 X5 Y{print_bed_max[1]*0.8} F{travel_speed*60} ; present print
{if max_layer_z < printable_height-10}G1 Z{z_offset+min(max_layer_z+70, printable_height-10)} F600 ; Move print head further up{endif}
{if max_layer_z < max_print_height*0.6}G1 Z{printable_height*0.6} F600 ; Move print head further up{endif}
M140 S0 ; turn off heatbed
M104 S0 ; turn off temperature
M107 ; turn off fan
M84 X Y E ; disable motors'''
machine_max_acceleration_e = "5000,5000"
machine_max_acceleration_extruding = "500,500"
machine_max_acceleration_retracting = "1000,1000"
machine_max_acceleration_travel = "1500,1250"
machine_max_acceleration_x = "500,500"
machine_max_acceleration_y = "500,500"
machine_max_acceleration_z = "100,100"
machine_max_jerk_e = "5,5"
machine_max_jerk_x = "8,8"
machine_max_jerk_y = "8,8"
machine_max_jerk_z = "0.4,0.4"
machine_max_junction_deviation = "0.013,0"
machine_max_speed_e = "60,60"
machine_max_speed_x = "500,500"
machine_max_speed_y = "500,500"
machine_max_speed_z = "10,10"
machine_min_extruding_rate = "0,0"
machine_min_travel_rate = "0,0"
machine_pause_gcode = "M25"
machine_start_gcode = '''G90 ; use absolute coordinates
M83 ; extruder relative mode
M140 S[bed_temperature_initial_layer_single] ; set final bed temp
M104 S150 ; set temporary nozzle temp to prevent oozing during homing
G4 S10 ; allow partial nozzle warmup
G28 ; home all axis
G1 Z50 F240
G1 X2 Y10 F3000
M104 S[nozzle_temperature_initial_layer] ; set final nozzle temp
M190 S[bed_temperature_initial_layer_single] ; wait for bed temp to stabilize
M109 S[nozzle_temperature_initial_layer] ; wait for nozzle temp to stabilize
G1 Z0.28 F240
G92 E0
G1 Y140 E10 F1500 ; prime the nozzle
G1 X2.3 F5000
G92 E0
G1 Y10 E10 F1200 ; prime the nozzle
G92 E0'''
printable_area = "0x0,220x0,220x220,0x220"
printable_height = "270"
printer_model = "Creality Ender-3 S1 (Klipper)"
printer_structure = "i3"
printer_technology = "FFF"
scan_first_layer = "0"
silent_mode = "0"
single_extruder_multi_material = "1"
support_air_filtration = "0"
support_chamber_temp_control = "0"
