model = "Bambu A1 mini"
slot_count = 4
# Bed identities match libslic3r's `curr_bed_type` enum vocabulary
# (`s_keys_map_BedType` in PrintConfig.cpp). Each entry has a
# corresponding bed.toml at `profiles/vendor/bbl/printer/
# bambu-lab-a1-mini/beds/<slug>.toml`.
supported_build_plates = [
    "Cool Plate",
    "Textured PEI Plate",
    "High Temp Plate",
    "Engineering Plate",
    "Supertack Plate",
]

[[toolheads]]
nozzle_diameter = 0.4
hotend_type = "stainless_steel"
max_temp = 300.0
slot_indices = [0, 1, 2, 3]

[build_volume]
min = [0.0, 0.0, 0.0]
max = [180.0, 180.0, 180.0]

exclusion_zones = []
