# Bambu Cool Plate SuperTack
#
# Hand-authored bed metadata (PR-S-4 rework). Bed.toml is intentionally
# thin: bed-temperature data lives per-filament-per-bed in the filament
# profile (`*_plate_temp` arrays); the cascade adapter (PR-1-6
# dimensional expansion) uses `curr_bed_type` to pick the right array
# index at slice time.

identity = "Bambu Cool Plate SuperTack"
curr_bed_type = "Supertack Plate"
